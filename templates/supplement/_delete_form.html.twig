<form method="post" action="{{ path('app_supplement_delete', {'idSupp': supplement.idSupp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ supplement.idSupp) }}">
    <button class="btn">Delete</button>
</form>
